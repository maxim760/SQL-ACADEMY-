Вывести средний возраст людей (в годах), хранящихся в базе данных. Результат округлите до целого в меньшую сторону.
Поля в результирующей таблице:
age
--
SELECT FLOOR(AVG(YEAR(FROM_DAYS(DATEDIFF(now(), birthday))))) as age FROM FamilyMembers
1) разница в днях
2) дата из этой разницы
3) колво полных лет из даты
4) среднее по таблице
5) округление вниз 
в скл академи не рабоатет если передать в дате дифф 3 аргумента
